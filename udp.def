Port := Bit16;
Length := Bit16;
Checksum := Bit16;

Packet := {
	src_port   : Port;
	dst_port   : Port;
	length     : Length;
	checksum   : Checksum;
	payload    : Bits;
} where {
	payload.width == (length*8)+32;
};

PDUs := {Packet};